package com.example.Classes;

import java.util.List;
import java.util.ArrayList;

class Pos {
    public int x;
    public int y;

    public Pos(int _x, int _y) {
        this.x = _x;
        this.y = _y;
    }
}

public class PosModel extends Model {
    // public List<Pos> posList = new ArrayList<Pos>();
    // public List<List<List<Pos>>> pos = new ArrayList<List<List<Pos>>>();
    public Pos[][][] pos;

    public PosModel() {
        // posList.add(new Pos(1, 1));
        // posList.add(new Pos(2, 1));
        // posList.add(new Pos(3, 1));
        // posList.add(new Pos(4, 1));

        // pos.add(new List<)
    }

    public void PrintList() {
        // Pos val = pos.get(0).get(0).get(0);
    }
}
